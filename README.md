# 🕷️ Universal AI Web Scraper

A powerful scraping tool that uses **DrissionPage** to bypass bot detection (Zillow, Spotify, Amazon) and **Google Gemini AI** to extract structured data automatically.

## 🚀 Features
- **Stealth Mode:** Mimics human behavior to avoid blocking.
- **AI Extraction:** Automatically identifies products, songs, or listings and converts them to JSON.
- **Secure:** Uses environment variables for API keys.

## 🛠️ Installation
1. Clone the repository.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
