services:
  - type: web
    name: aria-backend
    runtime: docker
    plan: starter
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: RENDER
        value: "true"
      - key: CHROMIUM_PATH
        value: /usr/bin/chromium
      - key: PORT
        value: "8000"
